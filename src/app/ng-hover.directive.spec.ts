import { NgHoverDirective } from './ng-hover.directive';

describe('NgHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NgHoverDirective();
    expect(directive).toBeTruthy();
  });
});
