export interface TodoModel{
    id:number,
    tsk:string,
    active:boolean,
    completed:boolean,
    archived:boolean,
}
export interface TodoModelA{    
    tsk:string,
    active:boolean,
    completed:boolean,
    archived:boolean,
}